/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["img.ophim8.cc"], // Thêm tên miền của hình ảnh vào đây
  },
};

module.exports = nextConfig;
